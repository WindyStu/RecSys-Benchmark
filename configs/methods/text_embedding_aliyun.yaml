method_id: text_embedding_aliyun
display_name: Aliyun Text Embedding
method_type: component
source: baselines/letter/data_process
adapter: recsys_benchmark.adapters.command.CommandAdapter
supported_tasks: [sdsr, cdsr]
supported_eval_inputs: []
commands:
  prepare: ["python", "aliyun_text_emb.py", "--dataset", "{dataset.dataset_id}"]
environment:
  required:
    - ALIYUN_API_KEY
notes: API keys must come from environment variables.
